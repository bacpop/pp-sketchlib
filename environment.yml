name: pp_env
channels:
  - conda-forge
  - bioconda
  - defaults
  - r
dependencies:
  - python
  - pip
  - setuptools
  - numpy
  - scipy
  - docopt
  - cmake >= 3.12
  - pybind11
  - zlib
  - eigen
  - highfive
  - hdf5
  - h5py
  - libgomp
  - openblas
  - libgfortran-ng
  - nvcc_linux-64
  - cudatoolkit # This is pinned due to version install on azure, see azure-pipelines.yml
